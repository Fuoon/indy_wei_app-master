<header id="games-header-bar" class="navbar navbar-inverse">
	<div class="navbar-inner">
		<div class="navbar-header">
			<h1 id="games-header-text">Games</h1>
		</div>
		<%= form_tag url_for(controller: 'games', action: 'search'), method: 'get' do %>
	       <div style="float:right; padding-top:15px; padding-right: 10px; width: 25%;">
	       	<%= text_field_tag :search, params[:search], :id => 'search_field', style: 'margin-bottom: 0px;', type:'search', placeholder: 'search games' %>
	       </div>
	       <div style="float:right; padding-top: 10px;" >
	            <%= button_tag(type: "submit", class: "btn btn-default") do %>
	            	<i class="icon-search"></i>
	            <% end %>
	       </div>
    	<% end %>
	</div>
</header>

<div id="lightbox">
	<h1>
		<%= link_to show_all_action_games_path, {:style => 'color: #00FFFF;'} do %>
			Action <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @action_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>

<div id="lightbox">
	<h1>
		<%= link_to show_all_arcade_games_path, {:style => 'color: #40FF00;'} do %>
			Arcade <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @arcade_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>

<div id="lightbox">
	<h1>
		<%= link_to show_all_adventure_games_path, {:style => 'color: #CC2EFA;'} do %>
			Adventure <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @adventure_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>

<div id="lightbox">
	<h1>
		<%= link_to show_all_puzzle_games_path, {:style => 'color: #FA5858;'} do %>
			Puzzle <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @puzzle_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>

<div id="lightbox">
	<h1>
		<%= link_to show_all_rpg_games_path, {:style => 'color: #FFFF00;'} do %>
			RPG <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @rpg_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>

<div id="lightbox">
	<h1>
		<%= link_to show_all_shooter_games_path, {:style => 'color: #2E9AFE;'} do %>
			Shooter <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @shooter_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>

<div id="lightbox">
	<h1>
		<%= link_to show_all_sport_games_path, {:style => 'color: #FF0040;'} do %>
			Sport <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @sport_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>

<div id="lightbox">
	<h1>
		<%= link_to show_all_strategy_games_path, {:style => 'color: #FFFF00;'} do %>
			Strategy <i class="icon-plus icon-white"></i>
		<% end %>
	</h1>
	<ul>
	<% @strategy_games_list.each do |game| %>
		<li>
			<% if game.image_attachments.count > 0 %>
				<%= link_to image_tag(game.image_attachments.first.image_url), game %>
			<% end %>
			<div class="image_title">
				<h5 class="title"><%= link_to game.name, game %></h5>
			</div>
		</li>
	<% end %>
	</ul>
</div>
